import React from "react";
import Text from './text';

const Header = ()=>{
    return(
        <div id={`header`}>
            <Text text={'I am header'}/>
        </div>
    )
};

export default Header;