import React from "react";
import Text from './text';

const Footer = ()=>{
    return(
        <div id={`footer`}>
            <Text text={`I am footer`}/>
            
        </div>
    )
};

export default Footer;